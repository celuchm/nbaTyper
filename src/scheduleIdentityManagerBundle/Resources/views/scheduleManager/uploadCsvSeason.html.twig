{% extends 'base/base.html.twig' %}

{% block content %}
    <h1>dodaj sezon - plik csv</h1>
    <h2>{{ uploadTitle }}  </h2>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Create" />
    {{ form_end(form) }}

{% endblock %}
